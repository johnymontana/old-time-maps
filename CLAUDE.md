# CLAUDE.md

All project instructions live in [AGENTS.md](AGENTS.md) — read it before
touching anything. The short version: public-domain sources only, no
hand-picked control points (seeds are fine, fits are automated), honest
residuals in every About panel, the land's Indigenous history stated
plainly, generated `assets/` committed, `work/`/`dist/`/`vendor/`
gitignored, chassis edits go in `flathead/src` and propagate, and nothing
gets pushed without being asked.
